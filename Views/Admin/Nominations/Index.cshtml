@model List<MangoTaikaDistrict.Domain.Entities.Nomination>
@{
    ViewData["Title"] = "Gestion des Nominations";
    var groupes = ViewBag.Groupes as List<MangoTaikaDistrict.Domain.Entities.Groupe> ?? new();
    Guid? selectedGroupeId = ViewBag.SelectedGroupeId as Guid?;
    Guid? selectedScoutId = ViewBag.SelectedScoutId as Guid?;
    string start = ViewBag.Start as string ?? "";
    string end = ViewBag.End as string ?? "";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h2 class="fw-bold mb-1"><i class="bi bi-award me-2 text-warning"></i>Nominations</h2>
        <p class="text-muted mb-0">Gestion des fonctions et postes</p>
    </div>
    <div>
        <a asp-area="Admin" asp-controller="Nominations" asp-action="Create" class="btn btn-primary btn-modern me-2">
            <i class="bi bi-plus-circle me-1"></i>Nouvelle Nomination
        </a>
        <a asp-area="Admin" asp-controller="Nominations" asp-action="Import" class="btn btn-success btn-modern">
            <i class="bi bi-file-earmark-excel me-1"></i>Importer Excel
        </a>
    </div>
</div>

<!-- Filtres -->
<div class="card-modern card mb-4">
    <div class="card-body">
        <h5 class="card-title mb-3"><i class="bi bi-funnel me-2"></i>Filtres</h5>
        <form method="get" class="row g-3">
            <div class="col-md-4">
                <label class="form-label fw-semibold">Groupe</label>
                <select name="groupeId" class="form-select form-modern">
                    <option value="">-- Tous --</option>
                    @foreach (var g in groupes)
                    {
                        var isSelected = selectedGroupeId.HasValue && g.Id == selectedGroupeId.Value;
                        <option value="@g.Id" selected="@(isSelected ? "selected" : null)">@g.Nom</option>
                    }
                </select>
            </div>
            <div class="col-md-3">
                <label class="form-label fw-semibold">Début</label>
                <input type="date" name="start" value="@start" class="form-control form-modern" />
            </div>
            <div class="col-md-3">
                <label class="form-label fw-semibold">Fin</label>
                <input type="date" name="end" value="@end" class="form-control form-modern" />
            </div>
            <div class="col-md-2 d-flex align-items-end">
                <button type="submit" class="btn btn-primary btn-modern me-2 w-100">
                    <i class="bi bi-search me-1"></i>Filtrer
                </button>
            </div>
        </form>
        <div class="mt-3">
            <a asp-area="Admin" asp-controller="Nominations" asp-action="Index" class="btn btn-outline-secondary btn-sm">
                <i class="bi bi-arrow-clockwise me-1"></i>Réinitialiser
            </a>
            @if (selectedGroupeId.HasValue || !string.IsNullOrEmpty(start) || !string.IsNullOrEmpty(end))
            {
                var routeValues = new Dictionary<string, string>();
                if (selectedGroupeId.HasValue)
                    routeValues["groupeId"] = selectedGroupeId.Value.ToString();
                if (!string.IsNullOrEmpty(start))
                    routeValues["start"] = start;
                if (!string.IsNullOrEmpty(end))
                    routeValues["end"] = end;

                <a asp-area="Admin" asp-controller="Reports" asp-action="ExportNominationsExcel" asp-all-route-data="routeValues" 
                   class="btn btn-outline-success btn-sm ms-2">
                    <i class="bi bi-file-earmark-excel me-1"></i>Excel
                </a>
                <a asp-area="Admin" asp-controller="Reports" asp-action="ExportNominationsCsv" asp-all-route-data="routeValues" 
                   class="btn btn-outline-info btn-sm ms-2">
                    <i class="bi bi-filetype-csv me-1"></i>CSV
                </a>
                <a asp-area="Admin" asp-controller="Reports" asp-action="ExportNominationsPdfOfficial" asp-all-route-data="routeValues" 
                   class="btn btn-outline-danger btn-sm ms-2">
                    <i class="bi bi-file-pdf me-1"></i>PDF Officiel
                </a>
            }
            else
            {
                <a asp-area="Admin" asp-controller="Reports" asp-action="ExportNominationsExcel" 
                   class="btn btn-outline-success btn-sm ms-2">
                    <i class="bi bi-file-earmark-excel me-1"></i>Excel
                </a>
                <a asp-area="Admin" asp-controller="Reports" asp-action="ExportNominationsCsv" 
                   class="btn btn-outline-info btn-sm ms-2">
                    <i class="bi bi-filetype-csv me-1"></i>CSV
                </a>
                <a asp-area="Admin" asp-controller="Reports" asp-action="ExportNominationsPdfOfficial" 
                   class="btn btn-outline-danger btn-sm ms-2">
                    <i class="bi bi-file-pdf me-1"></i>PDF Officiel
                </a>
            }
        </div>
    </div>
</div>

<!-- Tableau -->
<div class="card-modern card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-modern table-hover align-middle mb-0">
                <thead>
                    <tr>
                        <th><i class="bi bi-person me-1"></i>Scout</th>
                        <th><i class="bi bi-people me-1"></i>Groupe</th>
                        <th><i class="bi bi-briefcase me-1"></i>Poste</th>
                        <th>Fonction</th>
                        <th><i class="bi bi-calendar me-1"></i>Date Début</th>
                        <th><i class="bi bi-calendar-check me-1"></i>Date Fin</th>
                        <th><i class="bi bi-info-circle me-1"></i>Statut</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Any())
                    {
                        @foreach (var n in Model)
                        {
                            <tr>
                                <td class="fw-semibold">@n.Scout?.Nom @n.Scout?.Prenoms</td>
                                <td><span class="badge bg-info">@n.Groupe?.Nom</span></td>
                                <td><span class="badge bg-primary">@n.Poste</span></td>
                                <td>@(n.Fonction ?? "-")</td>
                                <td>@n.DateDebut.ToString("dd/MM/yyyy")</td>
                                <td>@(n.DateFin?.ToString("dd/MM/yyyy") ?? "-")</td>
                                <td>
                                    @if (n.Statut == MangoTaikaDistrict.Domain.Enums.StatutNomination.VALIDEE)
                                    {
                                        <span class="badge bg-success"><i class="bi bi-check-circle me-1"></i>@n.Statut</span>
                                    }
                                    else if (n.Statut == MangoTaikaDistrict.Domain.Enums.StatutNomination.EN_ATTENTE || n.Statut == MangoTaikaDistrict.Domain.Enums.StatutNomination.SOUMISE)
                                    {
                                        <span class="badge bg-warning"><i class="bi bi-hourglass-split me-1"></i>@n.Statut</span>
                                    }
                                    else if (n.Statut == MangoTaikaDistrict.Domain.Enums.StatutNomination.REJETEE)
                                    {
                                        <span class="badge bg-danger"><i class="bi bi-x-circle me-1"></i>@n.Statut</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">@n.Statut</span>
                                    }
                                </td>
                                <td class="text-end">
                                    <div class="btn-group" role="group">
                                        <a asp-area="Admin" asp-controller="Nominations" asp-action="Details" asp-route-id="@n.Id" 
                                           class="btn btn-sm btn-outline-info" title="Détails">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                        <a asp-area="Admin" asp-controller="Nominations" asp-action="Edit" asp-route-id="@n.Id" 
                                           class="btn btn-sm btn-outline-primary" title="Modifier">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <a asp-area="Admin" asp-controller="Nominations" asp-action="Delete" asp-route-id="@n.Id" 
                                           class="btn btn-sm btn-outline-danger" title="Supprimer">
                                            <i class="bi bi-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="8" class="text-center py-5">
                                <i class="bi bi-inbox fs-1 text-muted d-block mb-2"></i>
                                <p class="text-muted">Aucune nomination trouvée</p>
                                <a asp-area="Admin" asp-controller="Nominations" asp-action="Create" class="btn btn-primary">
                                    <i class="bi bi-plus-circle me-1"></i>Ajouter une nomination
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
